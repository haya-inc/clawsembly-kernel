{
  "name": "clawsembly-kernel",
  "version": "0.0.0",
  "description": "A clean-room browser execution kernel for an unmodified OpenClaw artifact.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24.15.0"
  },
  "scripts": {
    "dev": "vite",
    "contract:generate": "node scripts/generate-openclaw-sqlite-contract.mjs",
    "contract:check": "node scripts/generate-openclaw-sqlite-contract.mjs --check",
    "typecheck": "tsc -p tsconfig.app.json && tsc -p tsconfig.worker.json && tsc -p tsconfig.node.json",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "test:browser": "playwright test",
    "check": "npm run contract:check && npm run build && npm run test:browser"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "3.53.0-build1"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "@types/node": "25.6.0",
    "typescript": "7.0.2",
    "vite": "8.1.5"
  }
}
